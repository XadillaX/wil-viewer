import * as React from 'react'
import { TLShapeUtil } from '@tldraw/core'
import type { LineShape } from './LineShape'

export const LineIndicator = TLShapeUtil.Indicator<LineShape>(() => {
  return (
    <></>
  );
});
